const {ethers,deployments} = require('hardhat');
async function main(){
    // Write scripts later
}
main().then(()=>{
    process.exit(0);
}).catch(err=>{
    console.error(err);
    process.exit(1);
})